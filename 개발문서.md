# 프롬프트 관리 프로그램 - 설계 및 개발 문서

이 문서는 `prompt_manager.py`를 어떻게, 왜 이렇게 설계했는지에 대한 근거를 정리한 문서입니다.

## 1. 개발 환경 확인

- Python 버전 요구사항: 3.10 이상
  - 확인 방법: 터미널에서 `python --version` 실행
  - 개발에 사용한 실제 버전: `Python 3.14.7`
- Git 버전: `git --version` 실행 결과 → `git version 2.55.0.windows.5`
- Git 사용자 설정 확인 (`git config --global user.name` / `git config --global user.email` / `git config --global init.defaultBranch` 실행 결과):

```
user.name  = 최영진
user.email = shine2u4best@gmail.com
init.defaultBranch = main
```

## 2. 공개 샘플 저장소 Clone 확인

과제 요구사항에 따라 공개 샘플 저장소(`octocat/Hello-World`)를 clone하여 폴더 구조와 커밋 로그를 확인했습니다.

```
PS C:\Users\82103\OneDrive\바탕 화면> git clone https://github.com/octocat/Hello-World.git
Cloning into 'Hello-World'...
remote: Enumerating objects: 6, done.
...

PS C:\Users\82103\OneDrive\바탕 화면\Hello-World> dir

    디렉터리: C:\Users\82103\OneDrive\바탕 화면\Hello-World

Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
-a----          2026-09-24   오후 1:08         14 README

PS C:\Users\82103\OneDrive\바탕 화면\Hello-World> git log --oneline --graph
*   7fd1a60 (HEAD -> master, origin/master, origin/HEAD) Merge pull request #6 from Spaceghost/patch-1
|\
| * 7629413 New line at end of file. --Signed off by Spaceghost
|/
* 553c207 first commit
```

## 3. 브랜치 생성 및 병합 기록

`프롬프트 목록` 기능은 `feature/list` 브랜치에서 개발한 뒤 `main`에 병합했습니다.

작업 순서:
1. `git checkout -b feature/list` 로 새 브랜치 생성
2. `feature/list` 브랜치에서 `show_list()` 기능 구현 및 테스트 후 커밋
3. `git checkout main` 으로 다시 main 브랜치 이동
4. `git merge feature/list` 로 병합 (Fast-forward 병합)
5. `git push` 로 원격 저장소(GitHub)에 반영

아래는 이 저장소의 `git log --oneline --graph` 실행 결과이며, `feature/list` 브랜치 태그가 커밋 이력에 남아있어 브랜치 생성과 병합이 실제로 이루어졌음을 확인할 수 있습니다.

```
* da0938a (HEAD -> main, origin/main, origin/HEAD) Update README.md with test line
* 98f2e4b README 최종 작성 - 프로그램 설명, 실행 방법, 기능 목록 추가
* 98e81e9 즐겨찾기 목록 기능 구현
* 95002e6 즐겨찾기 관리 기능 구현
* 107a78e 프롬프트 상세 보기 기능 구현
* 5d50014 프롬프트 검색 기능 구현
* b6baa02 카테고리별 조회 기능 구현 및 전각 숫자 입력 처리 개선
* 94038b8 (feature/list) 프롬프트 목록 기능 구현
* 76c61ae 프롬프트 추가 기능 구현
* 4d0b202 기본 데이터 구조와 메뉴 뼈대 추가
* fcc800f Initial commit: 프로젝트 설정 (README, gitignore)
```

### 브랜치 전략

이 프로젝트는 기능(feature) 단위로 브랜치를 분리하는 것을 원칙으로 합니다. 하나의 기능이 완성되어 정상 동작이 확인되면 `main`으로 병합합니다. 이번 과제에서는 `프롬프트 목록` 기능을 `feature/list` 브랜치에서 개발해 브랜치 생성·병합 요구사항을 충족했습니다. 프로젝트 규모가 커질 경우, 모든 기능을 각자의 브랜치(`feature/기능이름`)에서 개발한 뒤 `main`에 병합하는 방식으로 확장할 수 있습니다.

### 병합 충돌 발생 시 대응 절차 (설계)

1. `git merge` 실행 시 충돌이 발생하면 Git이 충돌난 파일을 알려줍니다.
2. 해당 파일을 열어 `<<<<<<<`, `=======`, `>>>>>>>` 로 표시된 부분을 확인하고, 어느 쪽 코드를 남길지 비교합니다.
3. 두 변경 사항을 모두 반영해야 하면 직접 코드를 합치고, 한쪽만 유효하면 다른 쪽 코드를 지웁니다.
4. 충돌 표시를 모두 제거한 뒤 `git add`로 해결된 파일을 스테이징합니다.
5. `git commit`으로 병합을 완료하고, 프로그램을 다시 실행해 정상 동작을 검증합니다.

## 4. 함수 구조 및 책임

| 함수 | 책임 |
|---|---|
| `show_menu()` | 메뉴 항목을 화면에 출력 |
| `get_choice(prompt_text)` | 사용자 입력을 받아 전각 문자를 정규화(NFKC)하고 공백을 제거해 반환 |
| `add_prompt()` | 제목·내용·카테고리를 입력받아 새 프롬프트를 리스트에 추가 |
| `show_list()` | 전체 프롬프트 목록 출력 |
| `show_by_category()` | 선택한 카테고리에 속한 프롬프트만 필터링해 출력 |
| `search_prompt()` | 제목 또는 내용에 검색어가 포함된 프롬프트를 검색 |
| `show_detail()` | 프롬프트 하나를 선택해 전체 정보(제목/카테고리/즐겨찾기/내용)를 출력 |
| `toggle_favorite()` | 프롬프트의 즐겨찾기 상태를 켜고 끔 |
| `show_favorites()` | 즐겨찾기로 설정된 프롬프트만 출력 |
| `main()` | 전체 프로그램의 메뉴 반복 루프와 각 기능 호출을 담당 |

기능별로 함수를 분리한 이유는 메뉴 항목마다 서로 다른 책임을 가지기 때문입니다. 한 함수가 여러 역할을 하지 않도록 나누어, 특정 기능을 수정하거나 테스트할 때 다른 기능에 영향을 주지 않도록 설계했습니다.

## 5. 데이터 구조 설계

프롬프트 데이터는 "리스트(list) 안에 딕셔너리(dict)를 담는 구조"로 관리합니다.

```python
prompts = [
    {"title": "...", "content": "...", "category": "...", "favorite": False},
    ...
]
```

- **리스트를 사용한 이유**: 프롬프트가 여러 개이고 추가·순회(반복)가 잦기 때문에, 순서가 있는 컬렉션인 리스트가 적합합니다.
- **딕셔너리를 사용한 이유**: 프롬프트 하나가 제목·내용·카테고리·즐겨찾기 여부처럼 이름이 있는 여러 속성을 가지므로, 키(key)로 의미를 명확히 표현할 수 있는 딕셔너리가 적합합니다.
- **장점**: 구조가 단순해 코드 작성과 이해가 쉽고, 파이썬 내장 자료구조만으로 충분히 구현할 수 있습니다.
- **단점**: 데이터가 많아지면 검색할 때 리스트 전체를 순회해야 해 속도가 느려질 수 있고, 프로그램을 종료하면 데이터가 사라집니다(영속성 없음).

## 6. 입력 검증 로직

- `add_prompt()`의 제목·내용은 빈 문자열이면 다시 입력받도록 `while True` 반복문으로 검증합니다. 빈 값으로 프롬프트가 등록되는 것을 막기 위함입니다.
- 메뉴·카테고리 번호 입력은 `get_choice()`에서 `unicodedata.normalize("NFKC", ...)`로 전각 숫자(예: '３')를 반각 숫자('3')로 정규화한 뒤 비교합니다. 한글 입력기 환경에서 숫자가 전각으로 입력되어 메뉴 선택이 실패하는 문제를 방지하기 위한 처리입니다.
- 상세 보기·즐겨찾기 선택 시 `isdigit()`과 범위 검사(`1 <= n <= len(prompts)`)로 잘못된 번호 입력을 걸러냅니다.

## 7. 메인 루프 설계

`main()`은 `while True` 무한 루프로 메뉴를 반복 출력합니다. 사용자가 `0`을 입력할 때만 `break`로 루프를 탈출해 프로그램을 종료합니다. 그 외 유효한 번호(1~7)는 각 기능 함수를 호출한 뒤 다시 메뉴로 돌아오고, 잘못된 번호는 안내 메시지 후 다시 메뉴로 돌아옵니다. 사용자가 명시적으로 종료를 선택하기 전까지 여러 기능을 자유롭게 반복 사용할 수 있도록 이렇게 설계했습니다.

## 8. 검색 기능 설계

`search_prompt()`는 `in` 연산자를 이용한 부분 문자열 포함 검사로 구현했습니다. 제목 또는 내용 중 하나라도 검색어를 포함하면 결과에 포함시킵니다. 완전 일치가 아닌 포함(부분 일치) 방식을 선택한 이유는, 사용자가 검색어의 일부만 기억해도 원하는 프롬프트를 찾을 수 있도록 하기 위함입니다. 대소문자 구분 처리는 별도로 하지 않았는데, 데이터가 한글 위주라 대소문자 이슈의 영향이 적기 때문입니다.

## 9. 커밋 규칙

이 저장소는 기능 단위로 커밋을 분리하는 것을 원칙으로 합니다. 하나의 커밋은 하나의 완결된 기능(또는 문서 수정)만 담당하며, 커밋 메시지는 "무엇을 구현·수정했는지"를 한글로 간결하게 작성합니다. (예: "프롬프트 검색 기능 구현", "즐겨찾기 관리 기능 구현") 이렇게 하면 `git log`만 보고도 프로젝트가 어떤 순서로 만들어졌는지 파악할 수 있습니다.

## 10. 데이터 영속화 계획 (보너스 1 — 구현 완료)

과제 기본 요구사항은 "프로그램 실행 중에만 데이터 유지, 종료 시 초기화"였지만, 보너스 과제로 아래와 같이 실제 파일 저장 기능을 구현했습니다.

- **저장 형식**: JSON — 파이썬 리스트·딕셔너리 구조를 그대로 직렬화할 수 있고, 사람이 읽기도 쉬워 디버깅이 편리하기 때문에 CSV보다 JSON을 선택했습니다.
- **저장 시점**: `save_prompts()` 함수를 만들어서, 프롬프트 추가(`add_prompt()`) 및 즐겨찾기 변경(`toggle_favorite()`) 직후, 그리고 프로그램 종료(`0` 선택) 시에 `prompts.json`에 저장하도록 구현했습니다.
- **불러오기**: `load_prompts()` 함수가 프로그램 시작 시 `prompts.json`이 존재하면 그 내용을 불러오고, 없거나 파일이 손상된 경우에는 기본 프롬프트 3개(`DEFAULT_PROMPTS`)로 시작합니다.
- **내보내기**: 메뉴 "8. 프롬프트 내보내기"를 선택하면 `export_prompts_markdown()` 함수가 현재 등록된 프롬프트들을 카테고리별로 묶어서 `exports/` 폴더 안에 `카테고리이름.md` 형식의 Markdown 파일로 저장합니다. 각 파일에는 카테고리에 속한 프롬프트의 제목·즐겨찾기 여부·내용이 정리되어 출력됩니다.

## 11. 중복 제목 처리 정책

현재는 같은 제목의 프롬프트를 여러 개 등록하는 것을 막지 않습니다(제목 중복 허용). 목록·검색·상세보기에서는 등록 순서 번호로 구분하므로 사용자가 혼동하지 않도록 안내합니다. 추후 더 엄격한 관리가 필요하다면, 제목이 중복될 경우 "제목 (2)"처럼 자동으로 번호를 붙이는 방식을 도입할 수 있습니다.

## 12. 카테고리 관련 로직 수정 지점

카테고리 목록 자체를 변경하려면 파일 상단의 `CATEGORIES` 리스트만 수정하면 됩니다. `add_prompt()`와 `show_by_category()`는 모두 이 `CATEGORIES` 리스트를 참조해 번호를 보여주므로, 카테고리를 추가·삭제·순서 변경할 때는 `CATEGORIES` 한 곳만 고치면 프로그램 전체에 반영됩니다.
